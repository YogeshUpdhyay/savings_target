import 'package:flutter/material.dart';

class LandingSeqThree extends StatelessWidget {
  const LandingSeqThree({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Third page"),
    );
  }
}